# Targeted dark correction

Actual built-in image-tool request; see pair-review.md for the candidate and selected result.

Use case: precise-object-edit. Preserve this approved DARK neutral queue-action illustration exactly. Make only an output-canvas size correction: the input is 1585 by 992 pixels; output must be exactly 1586 by 992 pixels to match its approved light counterpart. Extend the far-right crop by one pixel consistently with the existing horizontal rows and background. Preserve all row positions, geometry, correct middle swipe offset, rounded corners, thumbnail squares, label bars, queue glyph, and neutral colors. Do not restyle or regenerate the scene, resize the objects, add shadows, add color, or alter the light-gray action tile. Return one PNG at 1586 x 992.
